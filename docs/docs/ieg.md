---
protected: true
---

# IEG 工作: NoSQL 管理端四项工程实践

## IEG 工作 1: 迁移类组件到函数组件

### 背景

IEG 的 ToB 项目是腾讯 NoSQL 的管理端, 页面上存在多个公共选择器, 历史上都是类组件实现:

- Set 集群选择器
- StandbySet 备用集群选择器
- Database 数据库选择器
- Table 表选择器 (该 NoSQL 的表格式是 xml)

工作 1 是把这些类组件迁移到函数组件. 迁移完成后, 配合性能优化 hooks 避免了重复请求, 优化了组件渲染性能, 并在过程中排查了闭包和接口响应时间不稳定导致的数据竞争.

### 为什么要迁移

- 生命周期逻辑分散: 类组件里一段副作用逻辑经常散落在 componentDidMount、componentDidUpdate、componentWillUnmount 三个方法里, 读代码要来回跳; 函数组件用 useEffect 把"执行副作用 + 清理副作用"收敛到一处, 按逻辑单元组织
- this 与绑定问题: 类组件的事件处理要么手动 bind, 要么写箭头函数属性, 容易踩 this 指向的坑; 函数组件全是普通闭包, 没有这类问题
- 逻辑复用能力: 选择器之间有大量共性逻辑 (拉取选项列表、缓存、联动刷新), 类组件只能靠继承或高阶组件复用, 函数组件可以抽成自定义 hook, 复用更直接、组合更灵活
- 性能优化手段更细: React.memo、useMemo、useCallback 可以精确控制重渲染和重复请求; 这也是迁移后"用性能优化 hooks 避免重复请求"的基础
- 生态与团队维护: 现代 React 生态 (状态管理、路由、组件库) 都以 hooks 为中心, 统一成函数组件后代码风格一致, 新人理解成本低; TypeScript 下函数组件 + hooks 的类型推导也比类的 this 类型更顺
- 面向未来: 并发特性 (如 useTransition、Suspense) 只对函数组件友好, 类组件是技术债方向

### 迁移有什么注意点

核心是生命周期与 hooks 的语义并不一一对应, 机械翻译会出 bug:

1. 生命周期到 useEffect 的映射

- componentDidMount 对应 useEffect(fn, []), 但注意 effect 是在浏览器绘制后异步执行, 和 componentDidMount 的时机有细微差别; 需要在绘制前同步读 DOM 的场景要用 useLayoutEffect
- componentDidUpdate 对应带依赖数组的 useEffect, 依赖必须列全, 漏列会拿到过期值
- componentWillUnmount 对应 effect 的清理函数 return () => {}
- shouldComponentUpdate 对应 React.memo 包裹组件, 配合 useMemo/useCallback 稳定引用
- getDerivedStateFromProps 这类"从 props 派生 state"的逻辑, 优先改成渲染期直接计算或 useMemo, 而不是再存一份 state
- 无法迁移的部分: 错误边界 (static getDerivedStateFromError, 配合 componentDidCatch) 没有 hook 等价物, 这类组件必须保留类组件

2. state 的形态变化

- this.state 是一个对象, setState 是合并且批量异步的; useState 是多个独立状态, 更新是整体替换. 迁移时如果照搬对象形态塞进一个 useState, 更新时漏掉字段就会丢数据

3. 闭包陷阱与数据竞争 (迁移中真实踩到的问题)

- 函数组件里事件处理和异步回调都是闭包, 捕获的是"当次渲染"的 state; 异步请求回来后再 setState, 可能基于过期值计算, 这就是闭包问题
- 选择器场景下接口响应时间不稳定: 用户连续切换筛选条件发出多个请求, 如果后发的请求先返回, 晚到的旧响应会覆盖新响应, 下拉选项和选中态错乱, 这是典型的数据竞争
- 对应的修法: effect 清理函数里标记本次请求作废 (或 AbortController 中断), 响应回来先校验"是否还是最新一次请求"再写入状态; 稳定的请求标识 + 竞态校验是选择器类组件的必备逻辑

4. 避免重复请求

- 迁移后利用性能优化 hooks 收敛请求: 依赖项用 useMemo 稳定化, 请求逻辑抽成自定义 hook, 同一选择器在多个面板挂载时共享缓存, 避免每个实例各发一次请求

5. 行为一致性验证

- 迁移是重构不是重写, 验收标准是行为完全一致: 交互流程、联动逻辑、边界 case 要逐条回归; 选择器之间还有级联关系 (选集群后才能选数据库、表), 一个组件迁移后联动方也要回归

### 如何使用大模型的能力进行迁移

选择器数量多、结构相似, 适合用大模型做批量迁移, 但不能无脑托管, 实际做法是"规范先行, 模型生成, 人工把关":

1. 先沉淀迁移规范, 作为给模型的约束

把生命周期映射表、state 拆分规则、竞态处理模式、命名与目录约定写成一份迁移规范文档, 每次迁移时作为上下文喂给模型, 保证多个组件的产出风格一致、映射规则统一.

2. 单组件批量转换

一次只迁移一个选择器: 把类组件源码 + 迁移规范交给模型, 让它产出函数组件版本. 选择器之间结构相似, 第一个组件人工精修后, 它的成品又可以作为后续组件的 few-shot 示例, 越迁越稳.

3. 人工 review 聚焦高风险点

模型的机械翻译 (JSX 结构、props 透传、事件绑定) 可信度高, 人工精力集中在模型容易错的地方:

- useEffect 依赖数组是否完整、有没有漏掉隐式依赖
- 异步回调里的闭包是否捕获了过期 state, 竞态校验是否存在
- setState 合并语义是否正确转换
- 清理函数的时机是否和原 componentWillUnmount 一致

4. 用模型辅助验证

让模型对照新旧两版代码逐条列出行为差异清单 (副作用时机、状态更新顺序、边界处理), 人工按清单回归测试; 也可以让模型根据原组件行为生成测试用例, 迁移前后各跑一遍对比.

5. 小步提交, 可回滚

一个选择器一个 PR, 迁移 + 回归通过再合入, 出问题可以单独回滚, 不影响其他组件.

### 性能优化 hooks: 如何避免重复请求、优化渲染

迁移完成后针对选择器做的第二轮优化, 直接动因是两类重复请求: 同一选择器在多个面板同时挂载, 每个实例各自发一次拉取选项的请求; 级联切换 (集群 -> 数据库 -> 表) 时, 传给请求 hook 的参数对象每次渲染都是新引用, hook 判定"参数变了"又发一次. 解法分三层:

1. 请求参数稳定化

把传给请求 hook 的参数收敛为原始值 (例如集群 id), hook 内部再用 useMemo 把参数组装成稳定对象, 依赖数组只放原始值, 保证"内容不变时引用不变". 参数引用不稳定是重复请求的最大来源, 这一步不做, 后面的缓存全部失效.

2. 请求逻辑抽成自定义 hook + 模块级缓存

拉取选项列表的逻辑从组件里抽成统一的自定义 hook, hook 内部读写模块级缓存 Map (key 为参数序列化后的字符串): 缓存命中直接返回, 未命中才发请求, 多个面板挂载同一选择器时共享同一份缓存. 再配一层在途请求去重, 相同 key 的请求还在飞时, 后来者复用同一个 Promise, 而不是并发再发一次. 缓存条目带过期时间, 或在选项数据有变更的入口 (新增、编辑) 主动失效对应 key, 避免拿到脏数据.

3. 渲染性能

选项列表的派生数据 (过滤、分组) 用 useMemo 缓存; onChange 等回调用 useCallback 稳定引用, 避免把新函数传给 React.memo 包裹的子组件导致 memo 失效; 选择器组件本身用 React.memo 包裹. memo / useMemo / useCallback 要成套配合, 任何一环引用不稳定, 整条 memo 链都会失效.

效果: 多面板挂载同一选择器时, 拉取请求从每个实例一次收敛为全局一次; 级联切换只请求真正变化的层级; 面板内其他状态变化引起重渲染时, 选择器子树基本被 memo 跳过.

### 小结

- 为什么迁: 收敛副作用逻辑、摆脱 this、hooks 复用、更细的性能优化手段、对齐现代 React 生态
- 注意点: 生命周期与 effect 语义差异、setState 合并语义变化、闭包过期值与请求竞态 (本项目真实排查过响应时间不稳定导致的数据竞争)、错误边界无法迁移、行为一致性回归
- 性能优化 hooks: 参数稳定化 + 自定义 hook 模块级缓存与在途请求去重, 多面板同选择器的请求收敛为一次; memo/useMemo/useCallback 成套使用控制重渲染
- 大模型用法: 迁移规范作为约束 -> 单组件生成 -> 人工聚焦依赖数组与竞态的 review -> 模型辅助列差异清单做回归 -> 小步提交可回滚

## IEG 工作 2: 内存泄漏排查、进程池与 ffi 内存模型

### 背景

IEG 的 NoSQL 管理端中, 表文件的格式是 xml, Koa 后端需要对这些文件做解密和解析. 解密与解析逻辑由 C++ 实现并编译成 .so 动态链接库, JS 侧通过 ffi-napi 调用. 这段工作有三条主线:

- 使用 valgrind 排查发现 C++ 侧 protobuf 的使用存在内存泄漏
- 为了避免解密、解析 xml 时崩溃 (例如 segfault) 影响主 Koa 进程, 使用 generic-pool 编写进程池, 把 .so 调用隔离到子进程
- 每次解析 xml 都需要创建一个大对象 (该对象的部分属性是一块大 buffer), 基于 V8 隐藏类创建对象池, 降低 Koa 服务的 GC 压力

下面按知识点逐个展开.

### 进程池、线程池的常用参数

无论进程池还是线程池, 参数模型基本一致, 以本项目使用的 generic-pool 为例:

- max: 池中 worker (进程/线程) 的数量上限. 决定并发处理能力的天花板
- min: 池常驻的最小数量. 进程池启动成本高 (fork 进程、加载 .so、初始化运行时), min 设为大于 0 可以避免冷启动毛刺
- acquireTimeoutMillis: 从池中获取 worker 的等待超时. 池满且排队超过该时间就抛错, 避免请求无限堆积; 上层要处理这个错误并给用户降级提示
- maxWaitingClients: 排队等待队列的上限, 和 acquireTimeout 一起构成背压机制
- idleTimeoutMillis: worker 空闲多久后被回收 (数量高于 min 的部分). 解析任务是突发型流量, 空闲回收可以释放内存
- evictionRunIntervalMillis / numTestsPerEvictionRun: 空闲驱逐检查的周期和每次检查的数量
- testOnBorrow / testOnReturn: 借出/归还时是否做健康检查. 进程池场景可以检查子进程是否还存活, 避免把已经崩溃的进程借给调用方
- fifo: 是否按先进先出复用空闲 worker

参数调优的思路:

- max 怎么定: 解密、解析是 CPU 密集型任务, 线程池的 max 通常参考 CPU 核数 (例如核数或核数 + 1); 进程池还要叠加内存约束, 每个子进程都独立加载 .so、持有解析 buffer, max 太大会把机器内存吃满, 需要按 单进程峰值内存 x max 估算
- min 怎么定: 看流量形态. 持续有请求就常驻 min 个进程避免冷启动; 明显有波峰波谷就让 min 小一些配合 idleTimeout 回收
- 超时怎么定: acquireTimeout 要大于单次解析的 P99 耗时, 否则正常慢请求会被误杀; 同时不能大到让调用方无限等待

进程池与线程池的选型差异, 也是本项目的关键决策:

- 线程池 (Node 侧对应 worker_threads) 共享进程地址空间, 通信快、开销小, 但任何一个线程里的 C++ 代码崩溃 (segfault、abort) 都会带走整个进程
- 进程池每个 worker 有独立地址空间, 子进程崩溃只影响自己, 主 Koa 进程感知到退出后销毁该 worker、重建新进程即可, 服务整体不受影响
- 本项目 .so 是第三方 C++ 代码, 存在内存泄漏这类不稳定因素, 崩溃隔离的收益大于进程间通信的开销, 所以选进程池

配套的 JS 侧对象池设计:

- 每次解析都要创建一个大对象 (部分属性是大 buffer), 高频创建会让 V8 频繁触发 GC, 大对象直接进入老生代, 回收代价高
- 做法是对象池: 预创建一批解析上下文对象, 用完重置状态后归还复用, 避免反复分配大 buffer
- 基于 V8 隐藏类 (hidden class / map) 的考虑: V8 对结构稳定的对象会生成相同的隐藏类, 属性访问走内联缓存, 速度快; 对象池里的对象保持相同的属性添加顺序和结构 (单态), 不动态增删属性, 既让访问快, 也让对象大小固定、复用安全

### valgrind 的原理、如何使用 valgrind 排查

valgrind 是一个动态分析框架, 最常用的是 memcheck 工具, 用于排查内存错误和内存泄漏.

原理:

- valgrind 采用动态二进制插桩 (dynamic binary instrumentation): 不修改源码、不需要重新编译, 程序运行在 valgrind 的虚拟 CPU 上, valgrind 在程序加载时把机器指令逐段翻译、插入检查代码后再执行
- memcheck 为程序的每一个内存字节维护两份影子状态: 该字节是否可寻址 (addressable, 即是否属于合法分配的内存), 以及该字节的值是否已定义 (defined). 所有内存读写指令都被插桩检查这两个状态
- 基于这套机制可以检测: 读写已释放的内存 (use after free)、越界读写、使用未初始化的值、new/delete 与 malloc/free 不匹配, 以及内存泄漏
- 泄漏检测的原理: 程序退出前, memcheck 扫描所有寄存器、栈和全局数据中仍然指向堆内存的指针, 把堆块分类:
  - definitely lost: 没有任何指针指向这块内存, 确定泄漏
  - indirectly lost: 指向它的内存本身也泄漏了
  - possibly lost: 指针指向块内部而不是块起始 (常见于数组或内部指针), 疑似泄漏
  - still reachable: 仍有指针可达, 通常不算问题 (很多库的全局对象属于这类)
- 代价: 插桩执行比原生慢一个数量级以上, 内存占用也显著增加, 适合定向排查, 不适合常态运行

使用方式:

```sh
# 直接跑 node 进程 (ffi-napi 加载的 .so 在同一进程内, 会被一起插桩)
valgrind --tool=memcheck \
  --leak-check=full \
  --show-leak-kinds=definite,possible \
  --track-origins=yes \
  --log-file=valgrind.log \
  node parse-xml.js

# 更干净的做法: 用 C++ 写一个最小复现程序, 只走 protobuf 解析路径
valgrind --leak-check=full --track-origins=yes ./parse_test table.xml
```

排查步骤, 以本项目为例:

1. 构造最小输入: 准备一份典型的 xml 表文件, 写一个只调用 .so 解密 + protobuf 解析的入口, 减少 Node 自身的噪音
2. 跑 valgrind 拿到报告: 关注 definitely lost 的条目, 每条都带分配点的调用栈
3. 读调用栈定位分配点: 报告的栈会指向 protobuf 内部的 Allocate 路径, 结合 C++ 侧代码确认是哪类对象没有释放. 本项目最终定位到 C++ 侧 protobuf 的使用问题: 解析产生的 message 对象在部分路径上没有正确释放, 每次解析都漏一点, 长时间运行后进程内存持续增长
4. 用 suppression 过滤无关噪音: Node、glibc 等运行时的已知泄漏可以写进 suppression 文件, 让报告聚焦业务代码
5. 修复后复测: 同样的输入再跑一遍, 确认 definitely lost 归零; 再长跑观察 RSS 是否稳定

补充对比: 如果能改 C++ 源码并重新编译, AddressSanitizer (ASan) 是更快的替代方案, 运行时开销远小于 valgrind, 但需要带 -fsanitize=address 重新构建; valgrind 的优势是零编译、对现成的 .so 二进制直接可用, 本项目排查第三方 .so 时这个特性很关键.

### ffi-napi 调用 .so: 是否共享内存, 该内存受 JS GC 控制吗

先说结论: 分情况. 标量参数是拷贝, 不共享; Buffer 和指针参数是共享内存; JS GC 只能管理 JS 侧分配的内存, C++ 侧分配的内存不受 JS GC 控制.

ffi-napi 的调用机制:

- ffi-napi 底层基于 libffi. JS 侧声明 C 函数签名 (参数类型、返回类型), ffi-napi 通过 dlopen 加载 .so、dlsym 找到符号, 调用时按 ABI 把参数组装成 C 能理解的布局再跳转执行
- 标量类型 (int、double 等): 调用时按值拷贝进寄存器/栈, C++ 侧拿到的是副本, 两边互不影响, 不存在共享

Buffer / 指针场景, 是真正的共享内存:

- JS 把 Buffer 传给 C++ 函数时, 传的是这块内存的地址, C++ 侧直接在这块内存上读写, 零拷贝; C++ 写入的内容 JS 侧立刻可见, 这正是解密、解析这类场景需要的: JS 分配 buffer, C++ 原地写入解析结果
- 共享带来两个必须注意的问题:
  - 生命周期: C++ 侧如果只在同步调用期间使用这块内存, 是安全的; 如果 C++ 把指针存下来异步使用, 而 JS 侧已经没人引用这个 Buffer, GC 可能回收它, C++ 侧就拿到悬垂指针. 规避方式是 JS 侧保持引用直到 C++ 用完, 或者 C++ 侧同步拷贝走
  - 小 Buffer 的池化陷阱: Node 对小于 Buffer.poolSize >>> 1 的 Buffer (poolSize 默认 8KB, 即默认 4KB 以下) 会从共享内存池里切片分配, 把这种切片的地址交给 C++ 有越界读写相邻数据的风险; 传给 native 的 buffer 建议用 Buffer.allocUnsafeSlow 或确保独立分配

C++ 侧分配的内存, 不受 JS GC 控制:

- 如果 .so 内部用 malloc/new 分配内存 (例如 protobuf 解析出的 message), 把指针返回给 JS, 这块内存完全在 V8 堆外, JS GC 看不见它、不会回收它
- 唯一的释放途径是 JS 侧显式再调一次 .so 提供的释放函数 (例如 destroy_message), 用 ffi 声明这个函数并在用完后调用
- 实践中可以用 try/finally 保证释放一定执行; 也可以用 FinalizationRegistry 在 JS 包装对象被 GC 时触发释放, 但 GC 时机不可控, 只能作为兜底, 不能作为主要手段
- 如果忘了释放, 表现就是 Node 进程 RSS 持续增长但 V8 堆内存正常, 这类泄漏要用 valgrind/ASan 在 native 侧排查, 用 heap snapshot 是查不到的

和本项目的呼应:

- 内存泄漏排查一节里, C++ 侧 protobuf 的泄漏正是这类"堆外内存不受 JS GC 管理"的典型: JS 侧看 GC 指标一切正常, 进程内存却在涨, 只能靠 valgrind 定位到 native 分配点
- 这也是选择进程池的另一个理由: 堆外泄漏和崩溃都发生在子进程里, 最坏情况下重启子进程就能恢复, 主 Koa 进程的内存曲线保持稳定

### 后续迁移到 C++ Addon: 优势与 tradeoff

ffi-napi 方案跑通之后, 随着解析调用量和数据量上升, 评估并迁移到了 C++ Addon (基于 N-API / node-addon-api) 的方案. 两者的定位差异决定了这次迁移的收益和代价.

C++ Addon 的优势:

- 调用开销更低: ffi-napi 每次调用都要经过 libffi 做运行时的参数组装 (JS 值 -> C ABI 布局), 类型转换全靠运行时描述; Addon 直接通过 N-API 读写 JS 值, 没有这层中间转换, 高频调用场景下差距明显
- 大数据零拷贝更自然: Addon 可以直接接收 ArrayBuffer 并在 C++ 侧原地处理, 解析结果也可以包成 external buffer 返回, 不需要像 ffi 那样围绕裸指针手工管理
- 内存管理可以和 GC 联动: N-API 支持给 external 内存挂 finalize 回调, JS 侧包装对象被 GC 回收时自动触发 C++ 侧释放. 这直接改善了 ffi 方案里"C++ 分配的内存不受 JS GC 控制, 只能手动释放"的问题, 堆外内存泄漏的敞口变小
- 异步能力完整: CPU 密集的解析可以用 AsyncWorker 丢进 libuv 线程池执行, 不阻塞事件循环; 跨线程回传结果有 ThreadSafeFunction 兜底. ffi-napi 的异步支持弱, 之前只能靠进程池顺带解决阻塞问题
- 类型表达更强: 可以直接在 C++ 侧构造 JS 对象、数组返回, 不用把 protobuf message 序列化成 buffer 再在 JS 侧二次解析, 省一次编解码
- ABI 稳定: N-API 保证跨 Node 版本 ABI 兼容, Addon 不需要跟随 Node 小版本反复重编 (这是老式 NAN addon 的痛点)

迁移的 tradeoff, 即 Addon 相比 ffi-napi 付出的代价:

- 编译与分发成本: ffi-napi 是纯 JS 层 + dlopen, 拿来即用; Addon 要走 node-gyp 构建, CI 需要维护编译工具链, 多平台、多 Node 版本要产出对应的 prebuild 产物, 发布链路明显变重
- 迭代耦合: .so 更新只需要替换二进制, JS 侧无感; Addon 的接口变更要同时改 C++ 和 JS 两侧, 并重新编译发布, 迭代节奏被绑在一起
- 崩溃隔离并没有被解决: 这是最容易误判的一点. Addon 运行在 Node 主进程地址空间内, C++ 侧 segfault 依然会带走整个进程. 所以迁移到 Addon 之后, 进程池的隔离层仍然保留, Addon 跑在池内的子进程里. Addon 解决的是性能和内存管理问题, 进程池解决的是故障隔离问题, 两者是叠加关系而不是替代关系
- 开发与调试门槛: 需要团队掌握 N-API 的引用计数、handle scope、ThreadSafeFunction 等概念, native 崩溃的排查依然要靠 valgrind/ASan 这套工具链

一句话总结这次迁移: ffi-napi 适合"快速接入一个现成的 .so、调用量不大"的阶段; 当调用频率、数据体积上来, 且希望堆外内存与 GC 联动时, N-API Addon 是更合适的形态; 而无论用哪种绑定方式, 第三方 C++ 代码的崩溃风险都要靠进程池兜底.

### 后续其他的迁移思路? (ffi-napi 调用 .so 链接库还能迁移到哪些形式?)

围绕"JS 进程里调用 .so"这条链路, 除 ffi-napi (运行时动态加载现成 .so) 和 N-API Addon (重写绑定层) 之外, 还有几类迁移形式, 各自的前提与代价不同:

1. napi-rs 等现代 Addon 工具链

把绑定层语言从 C++ 换成 Rust (napi-rs 框架), 形态上仍是 N-API Addon, 但工程化更好: 跨平台预编译产物的构建与发布由框架托管, 不用手写 node-gyp 的多平台编译矩阵, Rust 的内存安全也少一类 native bug. .so 本身不用动, Rust 侧通过 extern "C" 链接同样的符号. 与 C++ Addon 一样, 它不解决崩溃隔离, 进程池仍然要保留. 适用前提是团队具备 Rust 工程能力.

2. 独立解析服务 (服务化)

把 .so 包进一个常驻的解析服务 (C++ / Go / Node 实现均可), Koa 通过 rpc / http 调用. 相当于把隔离层级从"同机子进程"升级为"独立服务": 崩溃与内存泄漏被圈在服务内, 可以独立扩容、独立发布、独立重启, 多个业务方还能复用同一份解析能力. 代价是多一跳网络和一次序列化 (大 buffer 的传输成本明显), 多一个服务的部署、监控与排障成本; 同机部署时可以用 unix domain socket 降低开销. 适用前提是解析量大到需要独立伸缩, 或解析能力要被多个服务共用.

3. WASM 编译

如果能拿到 C++ 源码, 可以把解密、解析逻辑编译成 WebAssembly 在进程内运行: 线性内存边界受控, 越界表现为 wasm trap 而不是 segfault 崩掉进程, 也省掉了 node-gyp 的多平台编译产物. 关键限制: 现成的 .so 二进制无法直接转 wasm, 必须有源码且代码能过 Emscripten 工具链 (文件 IO、线程、系统调用都要适配), 性能通常低于原生. 对本项目"第三方只有 .so、没有源码"的前提不适用, 属于源码可得时才成立的备选.

4. 去掉 native 依赖, 纯 JS 重写

xml 解析与流式解密在 JS 生态都有成熟实现, 全部上移到 JS 可以一次性消灭绑定成本、崩溃风险与堆外内存问题, 类型也能直接对齐 TypeScript. 代价是 CPU 密集场景性能可能不够, 解密逻辑要与 C++ 侧逐位兼容 (padding、块模式、字节序), 迁移验证成本高. 适用前提是解析量不大, 或压测证明瓶颈并不在 native.

5. 命令行子进程封装

把 .so 包成一个小可执行程序, Koa 用 child_process.spawn 调用, 数据走 stdin/stdout 或临时文件交换. 实现最简单, 崩溃隔离天然成立; 代价是每次调用都有进程创建开销, 大 buffer 走 IPC 的序列化成本高. 本质上是进程池的"无池化退化版", 只适合低频、粗粒度的调用.

怎么选, 三个判断维度: 有没有源码 (没有源码就排除 WASM 与纯 JS 重写, 只能在绑定方式与服务化之间选)、调用量与数据量 (决定对调用开销和序列化开销的敏感度)、对崩溃与内存风险的容忍度 (决定隔离层级: 进程内 -> 子进程 -> 独立服务). 本项目的实际路径 ffi-napi -> 进程池隔离 -> N-API Addon, 是"无源码、调用量上升、必须保隔离"约束下的演进; 约束变化时, 上面的形式就是对应的分支: 拿到源码优先评估 WASM 或纯 JS 重写, 调用量继续涨或需要多方复用则走服务化.

## IEG 工作 3: TCP 连接池与连接异常处理

### 背景

IEG 的 NoSQL 管理端需要与 Tencent NoSQL DB Admin 通信, 协议是 TCP 长连接. Koa 后端使用 generic-pool 编写 TCP 连接池来管理与 DB Admin 的连接: 请求到来时从池中借出一条连接, 用完归还复用, 避免每次请求都走建连、握手、认证的完整流程.

连接池的价值:

- 省建连成本: TCP 三次握手加上 DB Admin 侧的认证握手, 每次现建连都是毫秒级开销, 高频请求下累积明显
- 控制连接数量: DB Admin 对单个来源的连接数有限制, 连接池把并发连接数收敛到 max 以内, 不会把对端打满
- 复用与预热: 常驻连接 (min) 让请求直接命中热连接, 延迟稳定

但 TCP 长连接不是"建好就永远可用"的, 连接池的核心难点正是异常处理: 连接失败、超时、对端主动关闭、静默断开, 每一种都要有明确的处理策略.

### 连接池的基本模型

generic-pool 围绕一个 factory 工作, factory 定义连接的生命周期:

```ts
const factory = {
  // 创建一条到 DB Admin 的连接: 建连 + 认证握手
  create: async () => {
    const conn = await connectToDBAdmin({ host, port, connectTimeout });
    await conn.handshake(auth);
    conn.on("error", onError); // 挂上错误监听, 避免未捕获错误打崩进程
    conn.on("close", onClose);
    return conn;
  },
  // 销毁连接: 彻底关闭 socket, 释放文件描述符
  destroy: async (conn) => {
    conn.destroy();
  },
  // 借出前校验连接是否可用 (testOnBorrow 开启时调用)
  validate: async (conn) => {
    return conn.alive && (await conn.ping(pingTimeout));
  },
};

const pool = genericPool.createPool(factory, {
  max: 10, // 连接数上限, 受 DB Admin 侧配额约束
  min: 2, // 常驻连接, 避免冷启动
  acquireTimeoutMillis: 3000, // 借不到连接的等待上限
  idleTimeoutMillis: 60_000, // 空闲连接回收
  testOnBorrow: true, // 借出前先校验
});
```

异常处理的所有策略, 都落在这三个函数和池参数的配合上.

### 异常一: 连接失败

场景: DB Admin 不可达 (网络故障、对端宕机、端口未监听), 或者认证握手被拒绝. 此时 factory.create 抛错.

处理:

- create 内部带连接超时: net.connect 本身可能长时间挂起 (例如被防火墙丢包, 既不通也不拒), 必须设置 connectTimeout, 超时主动 destroy 并抛错, 不能无限等
- 区分错误类型: 认证失败 (配置错误) 重试没有意义, 应该快速失败并告警; 网络类错误 (ECONNREFUSED、超时) 可以交给池的重建机制
- 池层面的行为: create 失败后, generic-pool 会把等待中的 acquire 请求按策略处理; 排队方在 acquireTimeoutMillis 内拿不到连接就抛错, Koa 侧捕获后返回降级响应 (例如"DB Admin 暂时不可用"), 而不是让请求挂死
- 不要在 create 里做无限重试: 重试应该由池的按需重建 + 上层有限次重试组成, create 内部死循环重试会把池卡死

### 异常二: 请求超时

场景: 连接本身是通的, 但某次请求发出去之后 DB Admin 迟迟不响应 (对端负载高、请求卡住、中间链路丢包).

处理:

- 请求级超时: 每次在连接上发请求都带超时计时, 超过阈值判定失败. 超时和连接失败要分开统计, 前者说明对端处理能力问题, 后者说明连通性问题
- 超时后的连接处置是关键: 请求超时意味着这条连接上的协议状态已经不可信 (对端可能稍后才把迟到的响应发回来, 污染下一个借用者的请求), 正确做法是 destroy 这条连接, 让池补一条新的, 而不是归还复用
- 概括成一条原则: 任何让连接状态变得不确定的错误, 一律销毁, 绝不归还. 只有明确"连接还干净"的场景 (例如业务层返回的错误码, 连接本身正常) 才归还
- 是否重试: 读类请求可以换一条连接重试一次; 写类请求要看 DB Admin 侧操作是否幂等, 不幂等就不能盲目重试, 避免重复执行

### 异常三: DB Admin 主动关闭连接

场景: DB Admin 侧有自己的空闲连接回收策略、例行重启、发布维护, 会主动关闭连接. JS 侧收到 socket 的 close 或 FIN.

处理:

- 连接上必须挂 error 和 close 事件监听: Node 的 socket 如果没有 error 监听器, 一次错误会直接抛未捕获异常打崩进程; 监听后把连接标记为不可用
- 池内感知: 连接被对端关闭后, 要么通过事件回调主动从池中驱逐 (pool.destroy), 要么依赖 testOnBorrow 在下次借出时用 ping 探活发现失效再销毁重建. 两种机制并存时, 事件驱动是主路径, 探活是兜底
- 规避对端空闲回收: 如果 DB Admin 的空闲超时是已知的 (例如 5 分钟), 池的 idleTimeoutMillis 应该设置得比对端更短, 让己方先回收空闲连接, 减少"借到一条对端已经关掉的连接"的概率; 也可以发应用层心跳保活
- 半关闭的细节: 对端发 FIN 只表示它不再发数据, 己方如果还持有写方向, socket 不会立刻触发 close 而是进入半关闭; 不过 Node 的 net.Socket 默认 allowHalfOpen=false, 收到 FIN 会自动 end 并触发 close, 半关闭只在显式开启 allowHalfOpen 时成立; 因此判断连接可用性不能只看"socket 没报错", 要结合协议层 ping 的结果

### 异常四: 静默断开 (最隐蔽的一类)

场景: 中间网络设备重启、NAT 映射过期、链路丢包, 连接两端都没有收到 FIN/RST, 从 JS 侧看这条连接"一切正常", 直到下次写数据才发现不通, 或者请求发出去石沉大海.

处理:

- TCP keepalive: 开启 socket 的 keepalive 并设置较短的探测间隔, 让操作系统层面尽早发现死连接; 注意默认的两小时探测间隔在生产环境基本没有意义, 必须调短
- 应用层心跳: 比 keepalive 更可靠, 定期在协议层发 ping 并期待 pong, 连续 N 次无响应就判定连接死亡并驱逐. keepalive 只能证明链路存在, 心跳能证明对端的应用层还活着
- 请求超时兜底: 即使前两者都漏了, 请求级超时仍然能保证调用方不会永久挂起, 超时后按"状态不确定"原则销毁连接
- 三层防御的关系: keepalive 和心跳负责"提前发现死连接", 请求超时负责"出了问题也能止损", 缺一不可

### 异常处理的通用原则汇总

- 连接状态不确定就销毁: 超时、半关闭、写失败之后, 连接一律 destroy, 由池重建, 绝不把脏连接归还给下一个借用者
- 连接级错误与请求级错误分开: 业务错误码 (例如查询的表不存在) 不影响连接可用性, 正常归还; 传输层错误才触发销毁重建
- 池自动补位: 销毁连接后, 池根据 min 配置按需重建, 保证常驻容量; create 失败不会让池缩容到无法恢复
- 背压与快速失败: acquireTimeout + maxWaitingClients 控制排队规模, 池满时快速报错, 避免请求在队列里无限堆积拖垮 Koa
- 有限重试 + 幂等前提: 失败请求换连接重试一次, 写操作以幂等为前提; 重试风暴比对端宕机本身更危险
- 可观测性: 暴露池的借用等待时长、排队数、创建失败率、销毁原因分布, 连接池的问题大多是从指标异常开始的

### 与进程池方案的呼应

工作 2 的进程池隔离的是"C++ .so 崩溃"这类进程内风险, 工作 3 的 TCP 连接池管理的是"跨进程、跨机器的连接生命周期"风险. 两者都用了 generic-pool, 但 factory 的语义完全不同: 前者的 worker 是子进程, 健康检查是进程存活; 后者的 worker 是 TCP 连接, 健康检查是协议层探活. 池化模型是通用的, 异常处理策略必须贴着被池化资源的特性设计.

## IEG 工作 4: 排查闭包引用导致的内存泄漏

### 背景

IEG 的 NoSQL 管理端在长时间运行后, Node 服务的堆内存持续缓慢增长, 不随 GC 回落. 排查后定位到闭包引用导致的内存泄漏: 一些函数闭包捕获了大对象的引用 (例如解析后的 xml 表数据、请求上下文), 而这些闭包被定时器、事件监听或长生命周期结构持有, 导致大对象永远可达、无法被 GC 回收.

### 闭包为什么会导致内存泄漏

先讲清楚机制:

- JS 的函数在创建时会捕获它所在作用域的变量引用, 形成闭包. V8 中体现为函数对象上挂着一个 context/scope 链, 指向它引用的外层变量集合
- V8 的 GC 是标记-清除 (mark and sweep): 从 GC roots (全局对象、执行栈、活跃的内部结构) 出发标记所有可达对象, 不可达的才回收
- 闭包本身只要被引用, 它捕获的变量就保持可达. 也就是说: 不是"对象被闭包引用"有问题, 而是"闭包被长生命周期的东西引用, 且闭包捕获了不该长期持有的大对象"

泄漏的成立需要两个条件同时满足:

1. 闭包捕获了大对象 (或会持续增长的结构)
2. 闭包被长生命周期的载体持有, 例如: 未清理的 setInterval/setTimeout、未移除的事件监听器、挂到全局或模块级变量上的回调、没有淘汰策略的缓存

只满足其一不会泄漏: 闭包捕获了大对象但闭包本身很快不可达, GC 正常回收; 闭包长期存活但只捕获小标量, 也没有实际影响.

### 典型泄漏模式

结合本项目和 Node 服务常见场景:

1. 定时器持有闭包

```js
// 泄漏: 定时器没有清理, 闭包一直活着, capturedData 永远可达
function startReport(capturedData) {
  setInterval(() => {
    report(capturedData.summary);
  }, 60_000);
}
```

2. 事件监听器未移除

```js
// 泄漏: 每次请求都注册监听, 从不 off/removeListener,
// 监听回调的闭包累积, 每个都捕获了当次请求的 ctx
emitter.on("refresh", () => {
  doSomething(ctx.parsedTable);
});
```

3. 闭包意外捕获大作用域

```js
function handle(hugeXml) {
  const parsed = parseXml(hugeXml); // 大对象
  registerCallback(() => {
    // 闭包只需要 tiny, 但 V8 的 scope 共享机制下,
    // 同一作用域里被其他闭包引用的 parsed 也可能被一并保留
    log(tiny);
  });
}
```

V8 会对同一作用域的变量做 context 共享: 只要该作用域里有任何一个变量被某个存活闭包引用, 整个 context 对象都可能被保留, 导致"闭包明明只用了小变量, 大对象却也漏了".

4. 缓存只进不出

```js
// 泄漏: Map 挂在全局, 闭包写入后没有任何淘汰路径
const cache = new Map();
function load(key, bigValue) {
  cache.set(key, bigValue);
}
```

5. 被遗忘的全局引用: 调试时把大对象挂到 global 或模块顶层变量, 上线后没有移除.

### 排查手段

1. 确认现象: 区分堆内泄漏还是堆外泄漏

- 用 process.memoryUsage() 观察 heapUsed 与 rss: heapUsed 持续涨说明是 V8 堆内问题 (闭包泄漏属于这类); rss 涨而 heapUsed 平稳则要往 native 侧查 (参考工作 2 的 valgrind 排查)

2. heap snapshot 三次快照对比法 (核心手段)

- 在 Chrome DevTools (node --inspect 连接) 或 memwatch-next、heapdump 这类堆分析工具里:
  - 快照 1: 服务稳定后拍一次
  - 执行可疑操作 N 次 (例如反复触发解析 xml 的请求)
  - 快照 2: 再拍一次
  - 手动触发 GC 后拍快照 3
- 用 Comparison 视图对比快照 1 和快照 3, 只看 "New" (新增且未被回收) 的对象; 泄漏的对象会成批出现
- 选中可疑对象看 Retainers (保留链): 保留链会一路指到 GC root, 闭包泄漏的保留链典型形态是 Object -> (closure) -> Function -> 某个全局的定时器/监听器/Map. 保留链的终点就是修复的入口

3. 定位闭包的捕获内容

- 在 snapshot 里 Function 节点可以看到它关联的 context 对象, 展开能看到闭包实际捕获了哪些变量, 确认是不是意外捕获了大对象

4. 辅助手段

- node --inspect-brk + Allocation instrumentation on timeline: 观察分配随时间累积的曲线, 适合定位"哪个操作路径在持续分配"
- 生产环境不方便接 DevTools 时, 可以周期性打 heapUsed 指标, 结合发布变更记录二分定位引入泄漏的版本

### 修复方式

按泄漏模式对症:

- 定时器: 保存句柄, 在明确的生命周期点 clearInterval/clearTimeout; 服务类对象要有 dispose 语义
- 事件监听: 注册和移除成对出现; Node 侧可以用 { once: true } 或 AbortController 统一管理移除
- 缓存: 加淘汰策略, 容量上限用 lru (参考 Tiktok 项目的 lru 实现), 或键是对象时改用 WeakMap, 让条目随键的回收自动消失
- 缩小闭包捕获面: 把大对象的使用收敛到局部, 注册回调前把需要的字段提取成小变量, 避免闭包拖着整个大作用域; 必要时把注册逻辑拆到独立函数, 切断与大作用域的联系
- 全局引用: 代码审查中禁止随手挂全局, 调试代码不进主干

修复后用同样的三次快照法复测: 执行 N 次操作后, Comparison 视图里不应再有该类型的 New 对象残留, heapUsed 曲线恢复锯齿形 (涨后被 GC 拉回).

### 与其他工作的关联

- 和工作 2 的区别: 工作 2 是 C++ 侧 protobuf 的堆外泄漏, RSS 涨而 heapUsed 平稳, 用 valgrind 排查; 本工作是 JS 堆内的闭包泄漏, heapUsed 涨, 用 heap snapshot 排查. 判断泄漏先分清在哪个堆
- 和函数组件迁移 (工作 1) 的关联: React 侧的闭包泄漏同样常见, useEffect 里注册的监听、定时器如果不在清理函数里释放, 组件卸载后闭包依然存活; 这也是迁移规范里要求严格检查清理函数的原因
