# 虚拟滚动: 为什么用, 为什么手写, 不定高怎么实现

## 背景

ToD 项目前端使用虚拟滚动列表渲染 Android 设备的设备性能分排行榜. 排行榜的数据经过 BFF 分页下发, 但单页以及滚动加载累积下来的条目数依然很多, 如果全量渲染成 DOM 节点, 页面会明显卡顿. 虚拟滚动是长列表场景的标准解法.

## 为什么要使用虚拟滚动可以提高首屏加载速度吗

虚拟滚动的核心思想: 列表容器保持完整的高度 (撑出和全量数据一致的滚动条), 但任意时刻只渲染可视区域 (外加上下少量缓冲区) 内的子项, 滚动时动态替换渲染集合.

它解决的问题是 DOM 节点数量:

- 渲染成本: 浏览器对每个 DOM 节点都要做样式计算、布局、绘制、合成, 1 万个节点的排行榜首次渲染可能要几百毫秒甚至秒级, 虚拟滚动后同一时刻只有几十个节点, 渲染耗时降到毫秒级
- 内存成本: 每个节点及其对应的 React Fiber、事件绑定都占内存, 节点少了内存占用和 GC 压力同步下降
- 交互成本: 节点少, 滚动时的重排重绘范围小, 帧率稳定; 配合 React 侧的优化 (只 diff 可视集合), 滚动不掉帧

那么它能提高首屏加载速度吗? 要拆开看:

- 能提高的部分: 首屏的渲染耗时 (FCP/LCP 中"把内容画出来"这一段). 即使首屏只展示 20 行, 不做虚拟滚动时浏览器也要把拿到的全部数据渲染成节点再绘制, 虚拟滚动让首屏只渲染可视区的几十个节点, 首次绘制显著变快, 主线程阻塞时间变短
- 不能提高的部分: 网络请求耗时、数据下载和 JSON.parse 耗时. 这部分要靠 BFF 的服务端分页、字段裁剪来解决 (见 4.md), 虚拟滚动管不到数据传输
- 一个常见误区: 虚拟滚动不是"少加载数据", 而是"少渲染节点"; 数据该拿多少还是多少 (或靠分页控制), 它优化的是渲染侧

结论: 虚拟滚动可以提高首屏渲染速度, 但完整的"首屏加载速度"优化是 服务端控制数据量 (分页/裁剪) + 客户端控制节点量 (虚拟滚动) 的组合.

## 为什么手写虚拟滚动而不是用-tanstack-react-virtual-等开源方案

先说清楚: 开源方案 (如 @tanstack/react-virtual) 本身质量很高, 覆盖了不定高、横向滚动、动态测量等大量边界情况, 一般项目直接用是更省力的选择. ToD 选择手写, 是基于当时场景的权衡:

- 业务形态高度定制: 排行榜不是纯列表, 有分组表头、排名变化标记、展开查看机型明细等定制交互, 滚动定位 (例如点击搜索后滚动到指定机型) 也需要精细控制; 用开源库做这些定制要不断绕它的抽象, 成本未必比自己写低
- 场景收敛, 实现可控: 列表是单列、纵向滚动、子项高度规则可预估, 核心逻辑 (可视区间计算 + 绝对定位 + 偏移) 两三百行就能写完, 团队完全掌握, 出问题能直接定位, 不需要去读第三方源码
- 依赖治理: 内部项目对第三方依赖有引入审核和版本维护成本, 为一个收敛场景引入并长期维护一个依赖, 不如沉淀成团队内部组件
- 性能调优空间: 自己实现可以针对场景做优化, 例如滚动事件用 passive 监听、用 rAF 节流、缓冲区大小按实测帧率调整, 这些在通用库里不一定有开关

反过来也要讲清楚手写的代价, 面试中主动说更可信:

- 边界情况要自己兜底: 快速滚动白屏、缩放 (zoom) 下的尺寸偏差、容器 resize、SSR 场景等, 开源库已经打磨过的细节自己都要补
- 长期维护成本转到了团队自己身上

所以准确的表述是: 不是"开源方案不好", 而是"在定制需求强、场景收敛、团队有能力维护的前提下, 手写的综合成本更低"; 如果是通用中后台列表, 直接用 @tanstack/react-virtual 才是对的.

## 如何实现子项不定高的虚拟滚动列表

定高列表很简单: 可视区间 = scrollTop / itemHeight 直接算出来. 不定高 (每条高度未知, 渲染后才知道) 需要解决三个问题: 总高度怎么撑、可视区间怎么找、真实高度怎么修正. 标准做法是 预估高度 + 渲染后实测 + 前缀和与二分查找:

1. 初始化预估高度

给每个子项一个预估高度 estimateHeight (例如按设计稿的典型高度), 维护一个高度缓存数组 heights, 未实测前都填预估值. 容器总高度 = 所有 heights 之和, 用它撑开滚动区域.

2. 用前缀和 + 二分查找定位可视区间

维护前缀和 prefix[i] = heights[0] + ... + heights[i - 1], 表示第 i 项的顶部偏移. 滚动时:

- startIndex = 二分查找第一个满足 prefix[i + 1] > scrollTop 的 i
- endIndex = 二分查找第一个满足 prefix[i] > scrollTop + viewportHeight 的 i

二分查找把定位复杂度从 O(n) 降到 O(log n), 滚动事件高频触发时很关键. 渲染集合取 [startIndex - buffer, endIndex + buffer], buffer 是上下缓冲条数, 用来缓解快速滚动白屏.

3. 渲染后实测, 修正高度缓存

子项渲染完成后用 ResizeObserver (或 ref + getBoundingClientRect) 测量真实高度; 与缓存不一致时更新 heights[i], 并重算受影响的前缀和与总高度. 这样越滚动过的位置越准, 滚动条长度和位置抖动逐渐收敛.

4. 子项定位

可视集合内的子项用绝对定位 (position: absolute, top = prefix[i]) 或者容器 padding/transform 偏移的方式摆放, 保证滚动时位置正确.

核心逻辑的伪代码:

```ts
// heights: 每项高度 (未实测为预估值); prefix: 前缀和
function findStartIndex(prefix: number[], scrollTop: number): number {
  // 二分: 找第一个顶部偏移超过 scrollTop 的项的前一项
  let lo = 0;
  let hi = prefix.length - 1;
  while (lo < hi) {
    const mid = (lo + hi + 1) >> 1;
    if (prefix[mid] <= scrollTop) lo = mid;
    else hi = mid - 1;
  }
  return lo;
}

// 实测回调: 子项渲染后上报真实高度
function onMeasure(index: number, realHeight: number) {
  if (heights[index] === realHeight) return;
  heights[index] = realHeight;
  rebuildPrefixFrom(index); // 重算 index 之后的前缀和与总高度
}
```

工程上还有几个细节:

- 前缀和的增量重算: 实测修正只影响该位置之后的前缀和, 从 index 开始往后重算即可, 不需要全量重建; 数据量特别大时可以用树状数组把单点更新 + 区间查询做到 O(log n)
- 向上滚动加载历史数据时, 要补偿 scrollTop, 否则视口内容会跳动
- 测量放在布局完成后 (ResizeObserver 或 useLayoutEffect), 避免拿到旧尺寸造成反复修正

## 小结

- 虚拟滚动通过"只渲染可视区节点"解决长列表的渲染、内存、交互成本; 它能加快首屏渲染, 但首屏的网络与解析耗时仍需服务端分页配合
- 手写还是用开源库, 取决于定制强度、场景收敛度和团队维护能力; ToD 因排行榜定制交互多、场景收敛而手写, 通用场景仍推荐开源方案
- 不定高虚拟滚动的标准实现: 预估高度撑总高, 前缀和 + 二分查找定位可视区间, 渲染后实测修正缓存, 逐步收敛到精确布局
