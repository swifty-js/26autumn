# Tiktok 设备性能数据采集: perfetto 与两种采集方法

## 项目背景

Tiktok 项目: 团队使用 perfetto 在海量 Android 端设备侧采集设备性能数据. perfetto 是 Android 官方的性能追踪系统 (systrace 的继任者), 由 traced 守护进程统一管理 trace 会话, 各类数据源 (data source) 把追踪数据写入共享内存中的环形缓冲区 (ring buffer), 再由系统聚合、落盘、上报.

Android 端设备侧的采集方法有两种:

1. 插桩慢函数
2. 抓栈慢函数

两者的本质区别是: 插桩是精确测量 (instrumentation), 抓栈是统计采样 (sampling).

## 采集方法一插桩慢函数

原理: 在目标函数的入口和出口处插入计时探针. 函数被调用时记录开始时间戳, 返回时记录结束时间戳, 两者相减得到该函数这一次调用的精确耗时. 实现方式有两类:

- 编译期插桩: 构建阶段通过字节码/编译插件, 在指定函数的入口、出口自动注入计时代码, 不需要手工改业务代码
- 运行时 hook: 利用 ART 提供的 method tracing 能力 (方法入口/出口回调), 对指定方法集合做 trace

每次调用都会产出一条带起止时间的记录, 聚合后就是 perfetto trace 里的 slice (时间片段), 可以精确看到每个函数每一次调用花了多久、调用栈关系如何.

适用场景: 已知瓶颈路径上的关键函数, 例如渲染、布局、反序列化等明确的慢函数, 需要精确到单次调用级别的耗时数据.

优点:

- 数据精确, 每一次调用都有真实的起止时间, 不依赖统计推断
- 可以拿到完整的调用关系和调用次数

缺点:

- 探针本身有开销 (时间戳读取、记录写入), 被插桩函数调用越频繁, 开销越大, 会改变被测程序的行为 (观察者效应)
- 只能看到被插桩的函数, 没有插桩的函数是盲区
- 海量设备侧长期采集时, 需要控制插桩范围, 通常只对重点函数开启

## 采集方法二抓栈慢函数

原理: 不做任何插桩, 由采集线程按固定频率采样 (例如每秒上千次), 对目标进程的调用栈做一次快照, 记录"这一瞬间, 每个线程的栈上有哪些函数". 把大量快照按函数聚合, 某个函数在样本中出现的次数越多, 说明它占用 CPU 的时间越长, 从而得到热点函数视图. Android 上的实现途径包括:

- ART 的采样式 profiling: 周期性挂起目标线程、抓取 Java 调用栈
- simpleperf / perf_event: 基于硬件 PMU 计数或定时器中断采样, 中断处理时抓取栈, Java 和 native 代码都能覆盖

适用场景: 未知瓶颈的全局摸底, 想知道"整体时间都花在哪了", 以及海量设备侧的常态化采集.

优点:

- 开销小且可控: 开销只取决于采样频率, 与被测函数的调用次数无关, 适合在海量设备上长期开启
- 覆盖面广: 不需要预先知道哪个函数慢, Java 和 native 都能采到

缺点:

- 结果是统计近似值: 样本数只能估算耗时占比, 不是精确时间
- 采样有粒度: 执行时间小于采样间隔的短函数容易被漏掉, 也可能出现样本归因偏差 (例如栈顶函数被过度计入)

## JS 侧的插桩与抓栈

背景: App 内嵌了 React Native 和小程序容器, 相当一部分业务逻辑运行在 JS 引擎 (Hermes / V8 / JavaScriptCore) 的 JS 线程上. 前面两种方法对 JS 都不直接适用: ART 的 method tracing 只能看到 Java 方法; simpleperf 虽然能采到 JS 引擎的 native 帧, 但看到的是引擎内部实现, 看不到业务 JS 的函数名. 因此 JS 侧需要独立的采集手段, 插桩和抓栈都可以做.

### JS 插桩

原理与 Java 侧一致: 在目标函数入口、出口注入计时代码, 拿到单次调用的精确耗时. 实现途径有两类:

- 编译期转换: 在 bundle 构建阶段用 Babel 插件对函数体做转换, 在函数入口插入记录开始时间戳的语句, 在正常返回和异常抛出的路径上记录结束时间并计算耗时. React Native 的 Metro 打包链路和小程序的构建链路都基于 Babel, 可以统一接入, 不需要手工改业务代码
- 运行时包装: 用高阶函数把关键函数包一层, 调用前后记录时间. 小程序场景下常用于统一包装 Page / Component 的生命周期回调 (onLoad、onShow 等) 和 setData 这类关键 API

采集到的耗时数据可以经由桥接转成 slice 写入 perfetto trace, 与 native 侧数据对齐时间轴, 也可以独立上报.

注意点:

- JS 是单线程的, 探针开销会直接阻塞业务执行, 对高频小函数 (渲染函数、每帧回调) 要谨慎插桩
- 线上 bundle 通常经过压缩混淆, 函数名和源码对不上, 需要配合 source map 还原

### JS 抓栈

原理: 利用 JS 引擎自带的采样式 CPU profiler, 由引擎按固定频率中断 JS 执行、抓取当前 JS 调用栈, 聚合样本得到热点函数视图. 不同引擎的实现途径:

- React Native 默认使用 Hermes 引擎: Hermes 内置 sampling profiler, 开启后周期性抓取 JS 栈, 产出的样本数据可以用 hermes-profile-transformer 转成 Chrome trace 格式, 与 native 侧的 perfetto trace 合并分析
- 小程序运行时 (V8 / JavaScriptCore): 通过 Inspector 调试协议开启 CPU profiling (Profiler.start / Profiler.stop), 引擎在执行过程中周期性采栈

注意点:

- JS 抓栈只能看到 JS 栈, 看不到耗时花在 native 模块里的部分 (JSI 调用、bridge 通信、原生渲染), 跨 JS / native 边界的瓶颈要结合 native 侧数据一起看
- 同样依赖 source map 还原压缩后的函数名
- 采样粒度问题与 native 侧一致: 执行时间小于采样间隔的短函数容易漏采

JS 侧两种方法的取舍与 native 侧相同: 插桩精确但开销随调用次数增长, 适合已知关键路径; 抓栈开销小且可控, 适合大盘常态采集. 实际链路中, JS 侧的采样数据与 Java / native 侧数据按时间轴对齐后统一上报, 才能完整还原一次卡顿中 JS、bridge、native 各阶段的耗时.

## 两种方法的对比与配合

| 维度     | 插桩慢函数                 | 抓栈慢函数                     |
| -------- | -------------------------- | ------------------------------ |
| 测量方式 | 精确测量 (instrumentation) | 统计采样 (sampling)            |
| 数据含义 | 每次调用的真实起止时间     | 函数出现在栈上的频率, 估算占比 |
| 开销     | 随被插桩函数的调用次数增长 | 只随采样频率增长, 整体可控     |
| 覆盖面   | 只有被插桩的函数           | 全栈可见, 包括 native          |
| 短函数   | 能精确捕捉                 | 容易漏采                       |
| 典型用途 | 已知慢函数的精确分析       | 未知瓶颈的全局发现             |

实际链路中两者是配合关系: 抓栈在海量设备上常态化运行, 用来发现热点、看大盘趋势; 发现疑似问题后, 再对相关函数做插桩, 拿到单次调用级别的精确数据用于定位.

## 采集之后的数据链路

设备侧采集到的性能数据上报后, 进入数据管道: Kafka 作为数据接入层做缓冲和削峰, 解耦上报端和消费端; 数据再落到多种存储, 各司其职:

- Hive: 离线数仓, 存全量历史数据, 做批量分析
- ClickHouse: 列式 OLAP 库, 支撑排行榜这类实时聚合查询
- MySQL: 存配置、元数据等关系型数据
- Redis: 热数据缓存, 减轻数据库读压力

后端从库中读取数据, 使用 lru 内存缓存, 对外提供 rpc 服务; 前端 BFF 层调用 rpc 加工数据, 最终渲染设备性能排行榜.

# Tiktok 数据链路: Kafka、Hive、ClickHouse、MySQL、Redis 的职责

## 背景与整体链路

Tiktok 项目使用 perfetto 在海量 Android 端设备侧采集设备性能数据. 设备侧采集的数据 (插桩慢函数、抓栈慢函数的采集结果) 上报后, 先落 Kafka, 再分发到 Hive、ClickHouse、MySQL、Redis 等多种存储, 后端从库中读取数据并提供 rpc 服务, 前端 BFF 层消费后渲染设备性能排行榜.

整体链路:

```
Android 设备侧采集 -> 上报网关 -> Kafka -> 消费任务分发
                                          -> Hive (离线全量)
                                          -> ClickHouse (实时聚合)
                                          -> MySQL (配置/元数据)
                                          -> Redis (热数据缓存)
                                          -> 后端 rpc 服务 -> BFF -> 前端页面
```

每个组件解决的是链路中不同性质的问题, 下面逐个分析.

## Kafka 数据接入与缓冲层

Kafka 是分布式消息队列, 在这条链路中充当数据接入层, 职责包括:

- 削峰填谷: 海量 Android 设备的上报流量存在明显的波峰波谷 (例如用户活跃时段), Kafka 先把数据持久化到 topic 中, 下游消费方按自己的处理能力匀速消费, 避免上报洪峰直接打垮数据库
- 解耦上报端与消费端: 设备侧只需要把数据写进 Kafka, 不需要知道下游有哪些存储; 后续新增一个存储 (例如再加一个分析库), 只需要新增一个消费组, 上报逻辑不用改
- 数据分发: 一份上报数据可以被多个消费组独立消费, 分别写入 Hive、ClickHouse 等不同存储, 这是"一份数据落多个库"的实现基础
- 可重放: Kafka 的数据按保留策略落盘保存, 如果某个下游消费任务出 bug 写错了数据, 可以重置消费位点重新消费、重新灌库

Kafka 在这里不承担查询职责, 它只是流动的管道, 真正的查询能力由后面的存储提供.

## Hive 离线数仓

Hive 是构建在 Hadoop 生态上的离线数仓, 用类 SQL (HiveQL) 查询存储在 HDFS 上的海量数据, 职责包括:

- 存全量历史数据: 设备性能数据量大且持续增长, Hive 依托 HDFS 可以低成本地存储 PB 级数据, 适合作为全量数据的"底座"
- 离线批量分析: 例如按机型、系统版本、App 版本维度做长周期的性能趋势分析、回溯分析, 这类查询对延迟不敏感, 但对数据全量性要求高
- 数据加工的上游: 可以在 Hive 上做 ETL, 把清洗、聚合后的结果再导出给在线库使用

Hive 的查询延迟是分钟级甚至更高, 不适合在线页面直接查询, 所以在线场景交给 ClickHouse 和 Redis.

## ClickHouse 实时分析库

ClickHouse 是列式存储的 OLAP 数据库, 在这条链路中承担在线可查的分析职责:

- 排行榜类聚合查询: 设备性能排行榜需要按机型聚合性能分、排序、分页, 这是典型的 group by + order by 场景; 列式存储只读取查询涉及的列, 配合向量化执行引擎, 可以在亿级数据上做到秒级甚至亚秒级响应
- 多维筛选分析: 前端页面支持按系统版本、App 版本、时间范围等维度筛选, ClickHouse 对这类宽表多维过滤 + 聚合的查询模式非常友好
- 数据压缩比高: 同一列的数据类型相同、取值相近, 列存压缩率远高于行存, 存储成本更低

ClickHouse 不擅长高频的单行更新和事务, 所以它只承接"写入后主要做聚合查询"的性能数据, 不存需要频繁修改的业务数据.

## MySQL 关系型业务数据

MySQL 是行式存储的事务型数据库 (OLTP), 在这条链路中存的是量不大但需要事务和关系建模的数据:

- 配置与元数据: 例如排行榜的配置、机型维表、采集任务的开关和参数等
- 需要事务保证的业务数据: 例如用户对某个榜单的订阅、标注等操作
- 点查场景: 按主键读取单条配置这类查询, MySQL 的索引点查又快又稳

性能明细数据不会放进 MySQL: 数据量级太大, 且聚合分析不是 OLTP 数据库的强项, 硬扛会把数据库拖垮.

## Redis 热数据缓存

Redis 是内存 KV 数据库, 在这条链路中是离数据库最近的一层缓存:

- 缓存热点查询结果: 排行榜头部数据、高频访问的机型聚合结果等放进 Redis, 请求直接命中内存, 不打到 ClickHouse / MySQL
- 降低数据库压力: 海量设备数据对应的查询 QPS 很高, 缓存层把大部分读流量挡掉, 数据库只承接缓存未命中的请求
- 支撑读多写少的场景: 性能榜单数据更新频率有限 (按批次刷新), 缓存失效策略简单, 命中率高

缓存之外, 后端进程内还有一层 lru 内存缓存, 与 Redis 组成多层缓存结构: 进程内 lru 命中则零网络开销, 未命中再查 Redis, 最后才落到数据库.

## 为什么需要这么多存储选型逻辑

本质是不同性质的数据、不同的读写模式, 没有一种存储能同时满足, 按职责拆开:

| 组件       | 类型      | 承接的数据             | 读写模式           | 延迟要求   |
| ---------- | --------- | ---------------------- | ------------------ | ---------- |
| Kafka      | 消息队列  | 流动中的上报数据       | 高吞吐写, 顺序消费 | 不直接查询 |
| Hive       | 离线数仓  | 全量历史明细           | 批量写, 离线分析   | 分钟级以上 |
| ClickHouse | 列式 OLAP | 性能明细数据           | 批量写, 聚合查询   | 秒级       |
| MySQL      | 行式 OLTP | 配置、元数据、业务数据 | 低频写, 点查, 事务 | 毫秒级     |
| Redis      | 内存 KV   | 热点查询结果           | 高频读, 低频写     | 亚毫秒级   |

选型逻辑可以概括为:

- 流量入口要能扛洪峰、能解耦、能重放 -> Kafka
- 全量历史数据要存得起、算得动 -> Hive
- 在线聚合分析要快 -> ClickHouse
- 小而精的关系型数据要事务和点查 -> MySQL
- 热点读流量要挡在数据库前面 -> Redis

## 与后端服务的衔接

后端 (Golang + Kitex 框架) 从上述存储读取数据, 进程内使用 lru 内存缓存进一步降低对 Redis 和数据库的访问, 对外通过 rpc 提供服务; BFF 层 (Nest.js) 调用 rpc 方法, 做数据清洗和整形后暴露 HTTP 接口给前端页面, 前端使用虚拟滚动列表渲染设备性能排行榜.

这样分层的收益: 每一层只处理自己擅长的负载, 任何一层的压力变化 (例如上报量翻倍、查询 QPS 翻倍) 都可以独立扩容, 不会互相拖垮.

# rpc 是什么, rpc 的优势是什么

## rpc 是什么

rpc (Remote Procedure Call, 远程过程调用) 是一种进程间通信方式: 调用方像调用本地函数一样调用另一台机器上服务的方法, 底层的网络传输、序列化、服务寻址等细节对业务代码透明.

例如后端提供一个查询设备性能排行榜的方法, BFF 层的代码写起来是:

```ts
const ranking = await client.getDeviceRanking({ model, page });
```

看起来是本地函数调用, 实际上参数被序列化后通过网络发给后端服务, 后端执行后把结果序列化传回, BFF 拿到反序列化后的结果. "远程调用伪装成本地调用"就是 rpc 的核心思想.

## 一次 rpc 调用的完整流程

1. 服务提供方用 IDL (接口描述语言, 例如 Thrift、Protobuf) 定义服务接口: 方法名、入参、出参的数据结构
2. 代码生成工具根据 IDL 生成服务端骨架 (skeleton) 和客户端存根 (stub)
3. 调用方调用客户端存根的方法, 存根把入参按协议序列化成二进制字节流
4. 字节流通过网络传输 (通常是长连接 + 连接多路复用) 发送到服务端
5. 服务端收到字节流, 反序列化成参数对象, 根据方法名分发到真正的业务实现
6. 业务执行完成, 结果按相反方向序列化、传输、反序列化, 返回给调用方

对调用方来说, 整个 3-6 步都被存根封装掉了, 代码层面只看到一次函数调用.

## rpc 的优势

- 契约先行, 类型安全: 接口由 IDL 定义, 是前后端 (服务间) 的强类型契约; 从 IDL 自动生成客户端代码, 字段改名、类型变更在编译期就能发现, 不需要人工对齐 JSON 文档
- 序列化性能好: Thrift、Protobuf 等二进制协议比 JSON 体积更小、编解码更快, 对高频内部调用尤其明显
- 长连接与连接复用: rpc 框架通常维护连接池, 一次建连后复用, 避免每次请求都走 TCP 建连、TLS 握手的开销; 单连接上还可以多路复用多个请求
- 内置服务治理: 服务注册与发现 (调用方不需要硬编码地址)、负载均衡、超时与重试、熔断限流、链路追踪等能力由框架统一提供, 业务代码不用重复造轮子
- 调用语义清晰: 方法级别的调用模型比"拼 URL + 约定 JSON 结构"更贴近业务代码的表达方式, 重构友好

## rpc 与 http 接口的对比

| 维度     | rpc (如 Kitex + Thrift)             | http + json                                     |
| -------- | ----------------------------------- | ----------------------------------------------- |
| 接口契约 | IDL 定义, 代码生成, 强类型          | 依赖文档或 OpenAPI, 约束较弱                    |
| 序列化   | 二进制, 体积小, 编解码快            | 文本 JSON, 可读性好但体积大                     |
| 连接模型 | 长连接 + 连接池, 复用由框架统一管理 | HTTP/1.1 默认 keep-alive, HTTP/2 长连接多路复用 |
| 服务治理 | 框架内置注册发现、负载均衡、熔断    | 需要网关或自建                                  |
| 通用性   | 多用于内部服务间调用                | 浏览器、开放 API 通用                           |
| 调试     | 需要专用工具                        | curl 即可, 调试直观                             |

两者不是替代关系: 内部服务间高频调用适合 rpc; 面向浏览器、第三方开放接口适合 http.

## Tiktok 项目中的 rpc 实践

Tiktok 项目的后端使用 Golang 和字节后端框架 Kitex 对外提供 rpc 服务. 接口用 Thrift IDL 定义, 团队把 Thrift 定义生成 npm 包提供给 BFF 层消费: BFF (Nest.js) 直接 import 生成的客户端代码调用后端 rpc 方法, 入参出参都有 TypeScript 类型, 后端接口变更时 BFF 在编译期就能感知.

这条链路上 rpc 的收益具体体现为:

- 后端的数据服务不止 Tiktok 前端一个消费方, 其他团队也通过 rpc 接口消费同一份能力, rpc 的契约模型让多消费方接入成本一致
- 设备性能数据查询 QPS 高, 二进制序列化和长连接降低了单次调用的开销
- BFF 与后端分属不同进程、不同语言运行时, IDL 生成的代码保证了两端的类型一致性

BFF 拿到 rpc 返回的数据后做清洗、整形, 再暴露 http 接口给前端页面渲染设备性能排行榜.

# 为什么要引入 BFF 层

## 背景两条链路的对比

Tiktok 项目实际的链路是:

```
数据库 -> 后端 (Go + Kitex) -> rpc -> 前端 BFF (Nest.js) -> http -> 前端页面 (React)
```

一个自然的疑问是: 为什么不省掉 BFF, 让后端直接暴露 http 接口给前端页面:

```
数据库 -> 后端 (Go + Kitex) -> http -> 前端页面 (React)
```

省掉一跳看起来更简单, 但在 Tiktok 这个场景下会带来三个问题: 后端接口被单一前端绑架、前端拿到的是未经加工的大块原始数据、渲染性能和首屏性能受损. 下面从这三个角度展开.

## 原因一后端服务有多个消费方 rpc 接口要保持通用

后端的数据服务不是只服务 Tiktok 前端一个消费方, 其他团队也会通过 rpc 接口消费同一份设备性能数据能力. 这决定了后端接口的形态:

- 后端 rpc 接口必须面向"数据能力"设计: 返回通用的、完整的、结构稳定的数据模型, 不能为某一个前端页面的展示需求定制字段
- 如果让后端直接出 http 接口给页面, 后端就要为这个页面写一套视图定制的接口; 明天另一个前端页面接入, 又要再加一套, 后端接口会随消费方数量膨胀, 且每套都耦合具体 UI
- 把"面向页面的适配"收敛到 BFF: 后端只维护一套通用 rpc 接口, 每个前端在各自的 BFF 里做适配, 后端接口变更时, 通过 Thrift IDL 生成的 npm 包在编译期就能被 BFF 感知, 影响面也可控

一句话: rpc 层解决"多个消费方如何共用一份后端能力", BFF 层解决"每个前端如何按自己的需要消费这份能力".

## 原因二数据清洗与整形

后端 rpc 返回的是通用数据模型, 和页面真正要渲染的视图模型之间有明显差距, 这部分加工工作放在 BFF:

- 字段裁剪与重命名: 通用模型字段多、命名面向数据域, 页面只需要其中一部分; BFF 裁剪后前端拿到的就是干净的视图模型
- 多接口编排: 一个页面往往需要组合多个 rpc 方法的结果 (例如排行榜数据 + 筛选维度配置 + 机型维表), BFF 可以并行调用后聚合成一次 http 响应, 前端不用发多个请求再自己拼
- 格式转换与派生计算: 例如时间戳转展示格式、性能分分档、环比变化计算等, 这些是展示逻辑, 放后端污染通用接口, 放前端则每个页面重复实现
- 变更隔离: 后端数据模型调整时, 只需要改 BFF 的适配层, 前端页面无感; 反过来页面改版需要新字段组合, 也只改 BFF, 不动后端

Tiktok 项目中 BFF 层 (Nest.js) 调用后端提供的 rpc 方法, 清洗数据后暴露 http 接口给前端, 正是这个分工.

## 原因三海量 Android 设备数据下的前端渲染性能

Tiktok 的数据源是海量 Android 设备上报的性能数据, 即使经过聚合, 排行榜、明细列表的数据量依然很大. BFF 在"控制下发给前端的数据量"上起关键作用:

- 服务端分页: BFF 按页向 rpc 取数, 一次 http 响应只下发当前页需要的条目, 而不是把全量数据推给浏览器
- 预聚合与降采样: 趋势图这类场景, BFF 可以把细粒度数据聚合成前端绘图需要的粒度 (例如按天、按机型分桶), 下发的点数从几十万降到几百
- 就近缓存: BFF 可以缓存加工后的结果 (配合后端的多层缓存), 高频页面的 http 响应直接从缓存出, 减少 rpc 调用和重复计算
- 减小传输体积: 裁剪字段 + 聚合之后, http 响应体显著变小, 下载和 JSON.parse 的耗时都下降

这些手段共同保证了前端侧的渲染性能: 前端拿到的数据量被控制在"刚好够渲染"的规模, 再配合虚拟滚动列表只渲染可视区域内的行, 设备性能排行榜这种长列表才能流畅滚动. 如果把后端 http 直连页面, 这些加工要么缺失, 要么被迫塞进后端通用接口或前端组件里, 两边都会被污染.

## 其他收益

除了上面三个主因, BFF 还带来:

- 协议适配: 浏览器只会说 http, 不会直接调 rpc; BFF 承担了 rpc -> http 的协议转换, 后端不需要为浏览器场景维护一套 http 服务
- 语言与团队分工: 后端是 Go 技术栈, 前端团队用 Node (Nest.js) 写 BFF, 数据加工逻辑由更贴近页面的团队维护, 迭代不需要排后端的发布窗口
- 安全边界: 内部 rpc 服务不直接暴露给浏览器, 面向公网的只有 BFF 的 http 接口, 鉴权、限流等可以在 BFF 统一处理

## BFF 的成本与边界

引入 BFF 不是免费的, 面试中也应该主动讲清楚代价:

- 多一跳网络与一次序列化/反序列化, 链路延迟增加
- 多一个服务要部署、监控、排障, 出问题时要区分是 BFF 还是后端的锅
- 如果 BFF 里沉淀了过重的业务逻辑, 会变成第二个后端, 职责失控

所以 BFF 的定位应该克制: 只做面向前端的适配 (编排、清洗、整形、缓存、协议转换), 核心业务逻辑和数据一致性仍然留在后端. Tiktok 的分工符合这个边界: 后端负责数据服务和 rpc 能力, BFF 只做加工和暴露 http.

## 小结

回到最初的问题, 不采用"数据库 -> 后端 -> http -> 前端页面"的直连方案, 是因为:

- 后端能力有多个消费方, 必须用通用 rpc 接口对外, 不能被单一前端的视图需求绑架
- 通用数据模型到页面视图模型之间的清洗、整形、编排工作, 需要一个专门的适配层承接
- 海量设备数据必须先在服务端做分页、聚合、裁剪, 前端才有渲染性能可言

BFF 就是这个适配层: 对上消费 rpc, 对下提供为页面定制的 http 接口.

# 虚拟滚动: 为什么用, 为什么手写, 不定高怎么实现

## 背景

Tiktok 项目前端使用虚拟滚动列表渲染 Android 设备的设备性能分排行榜. 排行榜的数据经过 BFF 分页下发, 但单页以及滚动加载累积下来的条目数依然很多, 如果全量渲染成 DOM 节点, 页面会明显卡顿. 虚拟滚动是长列表场景的标准解法.

## 为什么虚拟滚动可以提高首屏加载速度

虚拟滚动的核心思想: 列表容器保持完整的高度 (撑出和全量数据一致的滚动条), 但任意时刻只渲染可视区域 (外加上下少量缓冲区) 内的子项, 滚动时动态替换渲染集合.

它解决的问题是 DOM 节点数量:

- 渲染成本: 浏览器对每个 DOM 节点都要做样式计算、布局、绘制、合成, 1 万个节点的排行榜首次渲染可能要几百毫秒甚至秒级, 虚拟滚动后同一时刻只有几十个节点, 渲染耗时降到毫秒级
- 内存成本: 每个节点及其对应的 React Fiber、事件绑定都占内存, 节点少了内存占用和 GC 压力同步下降
- 交互成本: 节点少, 滚动时的重排重绘范围小, 帧率稳定; 配合 React 侧的优化 (只 diff 可视集合), 滚动不掉帧

那么它能提高首屏加载速度吗? 要拆开看:

- 能提高的部分: 首屏的渲染耗时 (FCP/LCP 中"把内容画出来"这一段). 即使首屏只展示 20 行, 不做虚拟滚动时浏览器也要把拿到的全部数据渲染成节点再绘制, 虚拟滚动让首屏只渲染可视区的几十个节点, 首次绘制显著变快, 主线程阻塞时间变短
- 不能提高的部分: 网络请求耗时、数据下载和 JSON.parse 耗时. 这部分要靠 BFF 的服务端分页、字段裁剪来解决 (见 4.md), 虚拟滚动管不到数据传输
- 一个常见误区: 虚拟滚动不是"少加载数据", 而是"少渲染节点"; 数据该拿多少还是多少 (或靠分页控制), 它优化的是渲染侧

结论: 虚拟滚动可以提高首屏渲染速度, 但完整的"首屏加载速度"优化是 服务端控制数据量 (分页/裁剪) + 客户端控制节点量 (虚拟滚动) 的组合.

## 为什么手写虚拟滚动而不是用 @tanstack/react-virtual 等开源方案

先说清楚: 开源方案 (如 @tanstack/react-virtual) 本身质量很高, 覆盖了不定高、横向滚动、动态测量等大量边界情况, 一般项目直接用是更省力的选择. Tiktok 选择手写, 是基于当时场景的权衡:

- 业务形态高度定制: 排行榜不是纯列表, 有分组表头、排名变化标记、展开查看机型明细等定制交互, 滚动定位 (例如点击搜索后滚动到指定机型) 也需要精细控制; 用开源库做这些定制要不断绕它的抽象, 成本未必比自己写低
- 场景收敛, 实现可控: 列表是单列、纵向滚动、子项高度规则可预估, 核心逻辑 (可视区间计算 + 绝对定位 + 偏移) 两三百行就能写完, 团队完全掌握, 出问题能直接定位, 不需要去读第三方源码
- 依赖治理: 内部项目对第三方依赖有引入审核和版本维护成本, 为一个收敛场景引入并长期维护一个依赖, 不如沉淀成团队内部组件
- 性能调优空间: 自己实现可以针对场景做优化, 例如滚动事件用 passive 监听、用 rAF 节流、缓冲区大小按实测帧率调整, 这些在通用库里不一定有开关

反过来也要讲清楚手写的代价, 面试中主动说更可信:

- 边界情况要自己兜底: 快速滚动白屏、缩放 (zoom) 下的尺寸偏差、容器 resize、SSR 场景等, 开源库已经打磨过的细节自己都要补
- 长期维护成本转到了团队自己身上

所以准确的表述是: 不是"开源方案不好", 而是"在定制需求强、场景收敛、团队有能力维护的前提下, 手写的综合成本更低"; 如果是通用中后台列表, 直接用 @tanstack/react-virtual 才是对的.

## 如何实现子项不定高的虚拟滚动列表

定高列表很简单: 可视区间 = scrollTop / itemHeight 直接算出来. 不定高 (每条高度未知, 渲染后才知道) 需要解决三个问题: 总高度怎么撑、可视区间怎么找、真实高度怎么修正. 标准做法是 预估高度 + 渲染后实测 + 前缀和与二分查找:

1. 初始化预估高度

给每个子项一个预估高度 estimateHeight (例如按设计稿的典型高度), 维护一个高度缓存数组 heights, 未实测前都填预估值. 容器总高度 = 所有 heights 之和, 用它撑开滚动区域.

2. 用前缀和 + 二分查找定位可视区间

维护前缀和 prefix[i] = heights[0] + ... + heights[i - 1], 表示第 i 项的顶部偏移. 滚动时:

- startIndex = 二分查找第一个满足 prefix[i + 1] > scrollTop 的 i
- endIndex = 二分查找第一个满足 prefix[i] > scrollTop + viewportHeight 的 i

二分查找把定位复杂度从 O(n) 降到 O(log n), 滚动事件高频触发时很关键. 渲染集合取 [startIndex - buffer, endIndex + buffer], buffer 是上下缓冲条数, 用来缓解快速滚动白屏.

3. 渲染后实测, 修正高度缓存

子项渲染完成后用 ResizeObserver (或 ref + getBoundingClientRect) 测量真实高度; 与缓存不一致时更新 heights[i], 并重算受影响的前缀和与总高度. 这样越滚动过的位置越准, 滚动条长度和位置抖动逐渐收敛.

4. 子项定位

可视集合内的子项用绝对定位 (position: absolute, top = prefix[i]) 或者容器 padding/transform 偏移的方式摆放, 保证滚动时位置正确.

核心逻辑的伪代码:

```ts
// heights: 每项高度 (未实测为预估值); prefix: 前缀和
function findStartIndex(prefix: number[], scrollTop: number): number {
  // 二分: 找第一个顶部偏移超过 scrollTop 的项的前一项
  let lo = 0;
  let hi = prefix.length - 1;
  while (lo < hi) {
    const mid = (lo + hi + 1) >> 1;
    if (prefix[mid] <= scrollTop) lo = mid;
    else hi = mid - 1;
  }
  return lo;
}

// 实测回调: 子项渲染后上报真实高度
function onMeasure(index: number, realHeight: number) {
  if (heights[index] === realHeight) return;
  heights[index] = realHeight;
  rebuildPrefixFrom(index); // 重算 index 之后的前缀和与总高度
}
```

工程上还有几个细节:

- 前缀和的增量重算: 实测修正只影响该位置之后的前缀和, 从 index 开始往后重算即可, 不需要全量重建; 数据量特别大时可以用树状数组把单点更新 + 区间查询做到 O(log n)
- 向上滚动加载历史数据时, 要补偿 scrollTop, 否则视口内容会跳动
- 测量放在布局完成后 (ResizeObserver 或 useLayoutEffect), 避免拿到旧尺寸造成反复修正

## 小结

- 虚拟滚动通过"只渲染可视区节点"解决长列表的渲染、内存、交互成本; 它能加快首屏渲染, 但首屏的网络与解析耗时仍需服务端分页配合
- 手写还是用开源库, 取决于定制强度、场景收敛度和团队维护能力; Tiktok 因排行榜定制交互多、场景收敛而手写, 通用场景仍推荐开源方案
- 不定高虚拟滚动的标准实现: 预估高度撑总高, 前缀和 + 二分查找定位可视区间, 渲染后实测修正缓存, 逐步收敛到精确布局
